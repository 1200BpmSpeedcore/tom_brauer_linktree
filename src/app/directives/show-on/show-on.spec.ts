import { ShowOnDirective } from './show-on';

describe('ShowOn', () => {
  it('should create an instance', () => {
    const directive = new ShowOnDirective();
    expect(directive).toBeTruthy();
  });
});
